<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.tsicoop.dxnode</groupId>
  <artifactId>tsi_dx_node</artifactId>
  <packaging>war</packaging>
  <name>TSI_DX_Node</name>
  <version>1.0-SNAPSHOT</version>
  <description>TSI DX Node</description>
  <build>
    <sourceDirectory>src</sourceDirectory>
    <finalName>${project.artifactId}</finalName>
    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.11.0</version>
        <configuration>
          <source>${maven.compiler.source}</source>
          <target>${maven.compiler.target}</target>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.5.0</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <finalName>dx-node-cli</finalName>
              <outputDirectory>${project.basedir}/bin</outputDirectory>
              <transformers>
                <transformer>
                  <mainClass>org.tsicoop.dxnode.cli.DXNodeCLI</mainClass>
                </transformer>
              </transformers>
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                  </excludes>
                </filter>
              </filters>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-war-plugin</artifactId>
        <version>3.4.0</version>
        <configuration>
          <webXml>web/WEB-INF/web.xml</webXml>
          <warName>${project.artifactId}</warName>
          <webResources>
            <resource>
              <directory>web</directory>
            </resource>
          </webResources>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-clean-plugin</artifactId>
        <version>3.3.2</version>
        <executions>
          <execution>
            <id>auto-clean</id>
            <phase>clean</phase>
            <goals>
              <goal>clean</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <dependencies>
    <dependency>
      <groupId>jakarta.servlet</groupId>
      <artifactId>jakarta.servlet-api</artifactId>
      <version>5.0.0</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>
  <properties>
    <commons-dbcp2.version>2.12.0</commons-dbcp2.version>
    <jetty.version>12.0.9</jetty.version>
    <jackson.version>2.17.1</jackson.version>
    <jakarta.servlet-api.version>5.0.0</jakarta.servlet-api.version>
    <json-simple.version>1.1.1</json-simple.version>
    <jjwt.version>0.11.5</jjwt.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <commons-lang3.version>3.14.0</commons-lang3.version>
    <commons-codec.version>1.16.1</commons-codec.version>
    <maven.compiler.source>15</maven.compiler.source>
    <jetty-maven-plugin.version>10.0.11</jetty-maven-plugin.version>
    <maven.compiler.target>15</maven.compiler.target>
    <json-schema-validator.version>1.3.1</json-schema-validator.version>
    <postgresql.version>42.7.3</postgresql.version>
    <commons-text.version>1.12.0</commons-text.version>
  </properties>
</project>
